import { ContentPageTitleDirective } from './content-page-title.directive';

describe('ContentPageTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentPageTitleDirective();
    expect(directive).toBeTruthy();
  });
});
