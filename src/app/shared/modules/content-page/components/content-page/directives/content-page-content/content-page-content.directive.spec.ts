import { ContentPageContentDirective } from './content-page-content.directive';

describe('ContentPageContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentPageContentDirective();
    expect(directive).toBeTruthy();
  });
});
